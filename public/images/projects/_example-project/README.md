This folder is a structural example only — not a real project, not
referenced by any project data. It exists so the folder-naming convention
is something you can look at, not just read about.

Copy this folder's structure (renaming it to your project's slug) when
you start a new project, and delete whichever `case-study/` subfolders
you don't need. See `../README.md` and `docs/CONTENT_GUIDE.md`.
